(function(){const $=s=>document.querySelector(s);const $$=s=>[...document.querySelectorAll(s)];const favs=(()=>{try{return JSON.parse(localStorage.getItem('virexo-favorites'))||[]}catch(e){return[]}})();const f=$('#dashFavorites');if(f)f.textContent=favs.length;$$('.dash-mode').forEach(btn=>btn.addEventListener('click',()=>{$$('.dash-mode').forEach(x=>x.classList.remove('active'));btn.classList.add('active');const provider=btn.dataset.dashMode==='provider';$$('.provider-only').forEach(x=>x.hidden=!provider)}));$('#dashBook')?.addEventListener('click',()=>alert('Consultation booking flow opened — choose a date and time on the Marketplace Workspace.'));$('#dashMessage')?.addEventListener('click',()=>$('#dashChat')?.scrollIntoView({behavior:'smooth'}));$('#dashChatForm')?.addEventListener('submit',e=>{e.preventDefault();const i=$('#dashChatInput'),t=i.value.trim();if(!t)return;$('#dashChat').insertAdjacentHTML('beforeend',`<div class="chat-bubble sent"><strong>You</strong><span>${t.replace(/[<>]/g,'')}</span></div>`);i.value='';});$$('.dash-provider button').forEach(b=>b.addEventListener('click',()=>location.href='index.html#providers'));})();