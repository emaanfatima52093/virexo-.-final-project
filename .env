# Virexo works locally without any paid API or domain.
NODE_ENV=development
PORT=3000
JWT_SECRET=change-this-local-secret
DATABASE_PATH=backend/database/virexo.db
CORS_ORIGIN=http://localhost:3000
# Optional only: OPENAI_API_KEY=...
# Optional only: SMTP_HOST=...
